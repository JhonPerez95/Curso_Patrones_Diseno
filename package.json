{
  "name": "patrones_diseno",
  "version": "1.0.0",
  "description": "Curso practico de patrones de diseno",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:js": "nodemon index.js",
    "start:ts": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "3.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.7.0",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
